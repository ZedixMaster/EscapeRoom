/// @description acquire_inventory_item(index)
/// @function activates an inventory item
/// @param index what item to activate
items[argument[0]].image_alpha = 1;
items[argument[0]].acquired    = true;